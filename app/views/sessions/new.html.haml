- if current_user
  = button_to "Sign out", logout_path
- else
  = button_to "Sign in with Google", "/auth/google_login"

  - if can_emulate_oauth2?
    = button_to "Emulate OAuth2 sign in", "/sessions/emulated_login"
